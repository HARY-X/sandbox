<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mod｜新規登録</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <div class="sign">
      <div class="container">
        <h1><a href="/signup">新規登録</a></h1>
        <div class="panel">
          <h2>DOOROPEN</h2>
          <% if (errors.length > 0) { %>
            <ul class="errors">
              <% errors.forEach(error => { %>
                <li><%= error %></li>
              <% }); %>
            </ul>
          <% } %>
          <form action="/signup" method="post">
            <p>ユーザー名</p>
            <input type="text" name="username">
            <p>メールアドレス</p>
            <input type="text" name="email">
            <p>パスワード</p>
            <input type="password" name="password">
            <input type="submit" value="登録する">
			<p>すでに登録済みの方は<a href="/login">ログイン</a>する</p>
            <a href="/list">一覧にもどる</a>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>